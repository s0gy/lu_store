# TODO

## 第一优先级

- 把首页继续改成更像商品工具首页
- 增加静态输入区块：尺寸、颜色、材料
- 先不接提交逻辑，只把页面骨架做出来

## 第二优先级

- 设计 `rules.json` 的数据结构
- 确定尺寸、颜色、材料的映射方式
- 确定生成文案的模板结构

## 第三优先级

- 接入表单提交
- 后端读取 `rules.json`
- 页面展示生成结果

## 部署优化

- 清理不需要提交的本地文件，例如 `.venv/`、`.idea/`
- 优化 `.gitignore`
- 后续考虑 `gunicorn + systemd`
- 后续考虑绑定域名

## 备注

当前项目重点是先把原型和业务轮廓做出来，不急着上数据库。
规则较少时，优先使用 JSON。
