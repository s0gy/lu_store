# lu_store

最小 Flask 原型站点。

## 本地启动

1. 创建并激活虚拟环境
2. 安装依赖
3. 启动应用

```powershell
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
python app.py
```

打开 `http://127.0.0.1:5000/` 查看页面。
